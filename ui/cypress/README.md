To run these tests, navigate to the /ui directory and run

`npm install`

and then

`npm run test:e2e`

This will start a docker container with the application and a test database running in it. It then runs the tests and tears down the container.